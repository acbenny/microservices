server:
  port: 0
spring:
  application:
    name: order-service
  cloud:
    consul:
      config:
        format: yaml
        fail-fast: false
      discovery:
        fail-fast: false
        instance-id: "${spring.application.name}:${server.port}"